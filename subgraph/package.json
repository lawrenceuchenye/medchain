{
  "name": "medchain_v0.0.1",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ medchain_v0.0.1",
    "create-local": "graph create --node http://localhost:8020/ medchain_v0.0.1",
    "remove-local": "graph remove --node http://localhost:8020/ medchain_v0.0.1",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 medchain_v0.0.1",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.85.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
